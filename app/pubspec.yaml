name: bookkeep_app
description: 极速记账 App — P0/P1 全功能（Spec v1.0）
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '^3.12.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  drift: ^2.20.0
  drift_flutter: 0.2.6 # 0.2.7 要求 drift >=2.24 的 isolateDebugLog，而 Dart 3.5.4 解析到 drift 2.23.1
  sqlcipher_flutter_libs: ^0.6.0
  sqlite3_flutter_libs: ^0.5.24
  flutter_secure_storage: ^9.2.2
  local_auth: ^2.2.0
  fl_chart: ^0.69.0
  table_calendar: ^3.1.2
  cryptography: ^2.7.0
  intl: ^0.19.0
  path_provider: ^2.1.3
  http: ^1.6.0
  uuid: ^4.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  drift_dev: ^2.20.0
  build_runner: ^2.4.9
  mocktail: ^1.0.3
  sqlite3: ^2.4.0

flutter:
  uses-material-design: true
  assets:
    - assets/seed/categories_seed.json
