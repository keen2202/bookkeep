# 08-审查修复核验报告（独立复核）

| 项目 | 值 |
|---|---|
| 核验日期 | 2026-08-07 |
| 核验依据 | 《06-审查修复Spec规格说明.md》v1.1 + 《07-审查修复任务分解.md》 |
| 核验方式 | 静态代码逐项比对（文件+行号取证）+ 服务端实测（npm install / npm test / tsc） |
| 环境限制 | 本机无 Flutter SDK，flutter analyze / flutter test 无法复跑；服务端集成测试需 PostgreSQL（本机无），仅单元套件可实测 |

---

## 1. 总体结论

22 项任务中：**19 项完全落实、3 项部分落实（存在与文档字面要求的形式偏差）、0 项完全未做**。
文档声称的"全部 22 项 completed"基本属实，但存在 **4 处文档表述与代码实际不符**、**1 处文档化流程未执行**（验收报告未回填）、以及若干**文档自认的设备/CI 待办验收项**。

服务端实测结果：
- `npm run build`（tsc）：**0 error，通过** ✅
- 单元套件（排除集成后）：**5 套件 25 用例全绿** ✅（与文档"25 单测全绿"一致）
- `npm test` 原样运行：**失败** ❌（详见 §3.2，Windows 下单引号正则失效）

---

## 2. 已完成项清单（核验通过）

| 任务 | 结论 | 关键证据 |
|---|---|---|
| BK-R-001 服务端事务封装 | ✅ 落实 | `server/src/db/tx.ts:9-29`（BEGIN/COMMIT/ROLLBACK/finally release）；`pool.ts:8` connect()；`books.routes.ts:57-66,131-146` 已事务化；`CONTRIBUTING.md:3-9` 写入约定 |
| BK-R-002 邀请原子化与角色收敛 | ✅ 落实 | 单语句原子 UPDATE+RETURNING `books.routes.ts:132-137`；空行 409 `:138,147-149`；PATCH 白名单仅 editor/viewer、owner→403 `:202-205` |
| BK-R-003 建账竞态与 pull 只读化 | ✅ 落实 | `book.middleware.ts:21` autoCreate 默认 false；pull 404 `sync.routes.ts:40`；部分唯一索引 `one_owner_per_book` `db/schema.ts:33-34` |
| BK-R-005 账本刷新总线 | ✅ 落实 | `core/ledger_version.dart` 存在（StateProvider+syncMergeBus）；报表/日历/账户/预算 provider 均 watch；四处写路径 bump |
| BK-R-006 周期/分期自动补跑 | ✅ 落实 | `main.dart:91-93` 首帧后异步补跑；`app.dart:47,71-75` resumed 重跑 |
| BK-R-007 启用 SQLCipher | ✅ 落实（设备 hexdump 待做） | `main.dart:122-134` 启动序列；`key_store.dart:22` 64 hex 强制；`database_encryption.dart:63` sqlcipher_export；`_ResetDatabaseApp` 兜底 |
| BK-R-008 同步引擎接线与登录 | ✅ 落实（双设备 e2e 待做） | `settings/account_sync_section.dart` 完整；`main.dart:97` 引擎启动；`sync_engine.dart:124-133` 401 单飞锁 |
| BK-R-009 pull 安全窗口与重放队列 | ✅ 落实 | 服务端 `op.service.ts:65` 2s 窗口 + server_time；客户端 `sync_merger.dart:66-103` pending_replay；迁移 v7 `database.dart:42,127-135` |
| BK-R-010 同步客户端完善 | ✅ 落实 | `sync_api.dart:87-89` DELETE/PATCH，`:106-111` {error} 解析；books_api 同步改造 |
| BK-R-011 共享账本落地 | ✅ 落实（e2e 待设备） | `books_page.dart:191-205` createLocalBook 缓存；`sync_merger.dart:30` 含 'book'；`book_switcher.dart:62-85` 角色角标 |
| BK-R-012 服务端安全基线 | ✅ 落实 | helmet/CORS/限流 `app.ts:27-33`、`auth.routes.ts:11-17`；JSON 错误中间件+404 `app.ts:45-63`；JWT_SECRET 拒启 `index.ts:10-13`；优雅停机 `:30-45`；时序抹平+密码上限 128 `auth.routes.ts:21-26,91` |
| BK-R-013 UI 结构重构 | ✅ 落实 | `app.dart:123` IndexedStack；四页面无内层 Scaffold 残留 |
| BK-R-014 数据生命周期 | ✅ 落实 | `backup_service.dart:35-38,59` 补 4 表+schemaVersion；`recurring_tables.dart:19` type 字段；`account_repository.dart:149-171` 批量快照；`import_service.dart:50` CSV 事务 |
| BK-R-016 UI 性能与记账核心路径 | ✅ 落实 | `amount_keyboard.dart:23-24` SafeArea；`quick_entry_controller.dart:127-173` busy 锁+try/finally；7 处 ListView.builder；`report_charts.dart:53,76-83,212-216` 图表整改 |
| BK-R-017 深色模式与设计系统 | ✅ 落实 | `shared/theme/app_theme.dart` buildTheme+AppColorsExtension；`app.dart:110-112` darkTheme+themeMode:system；`cashflow_chart.dart:40` 脱敏固定尺度 |
| BK-R-018 预算提醒闭环 | ✅ 落实 | `budget_progress_calculator.dart:34,48-49` 读 threshold；`budget_alert_service.dart`+`flutter_local_notifications ^22.2.0`（pubspec.yaml:31）；`budget_repository.dart:156` onConflict；编辑/删除+唯一性校验 `budget_edit_sheet.dart:84-139` |
| BK-R-019 多币种贯通 | ✅ 落实 | `account_edit_sheet.dart:138-144` 币种字段；`transaction_repository.dart:108-195` rate_snapshot；`currency_repository.dart:101,113` 废占位+isRateSet；`reports_repository.dart:56-77` 快照折算 |
| BK-R-020 可访问性与交互反馈 | ✅ 落实 | `category_edit_sheet.dart:122-134` 48dp+Semantics；`amount_keyboard.dart:63` HapticFeedback；`accounts_page.dart:87-99` more 图标；`backup_page.dart:32,210` busy 态 |
| BK-R-022 技术债清偿 | ✅ 落实 | 重复 createTransfer 已删；`csv_parsers.dart:48,131` 表头定位+整数解析；`recurring_engine.dart:73-75` 动态窗口；LWW 删除优先已写入 `wbspecs/02-Spec规格说明.md:45,48-49` |

---

## 3. 未完成或存在偏差项清单

### 3.1 BK-R-004 Repository DI 收口 —— 部分落实（轻微偏差）
- **落实部分**：`TransactionRepository` 已 `required this.bookId`（`transaction_repository.dart:17`），生产代码回退逻辑已消除；`recurringServiceProvider` 注入 currentBookId（`recurring_providers.dart:11-16`）。
- **偏差**：Checklist 声称"删除 kDefaultBookId 回退"，但常量本身仍保留于 `app/lib/core/constants/constants.dart:10`（注释声明仅作列默认值/测试回退）。功能风险已消除，但与"删除"的字面表述不符；文档将该项标记 [x] 有过度声明之嫌。

### 3.2 BK-R-021 并发测试与测试基建 —— 部分落实（实测发现真实缺陷）
- **落实部分**：并发用例齐全（双 owner `sync.routes.integration.test.ts:140-156`、邀请复用 `books.routes.integration.test.ts:141-191`、pull 安全窗口 `sync.routes.integration.test.ts:198-216`、refresh 轮换 `auth.routes.integration.test.ts:71-104`）；集成测试统一使用 `bookkeep_test` 独立库；80% coverageThreshold 存在（`jest.config.js:10-17`）。
- **偏差 1（形式）**：Checklist 声称"去除 --coverage=false"，实际 `package.json:10,15` 的 `test` 与 `test:integration` 脚本**仍保留** `--coverage=false`；`testPathIgnorePatterns` 未写入 `jest.config.js`（改为 CLI 参数，`jest.config.js:7-8` 有注释说明此决策）。
- **偏差 2（实质缺陷，实测复现）**：`npm test` 脚本中的单引号正则 `'...\.integration\.test\.ts$'` **在 Windows cmd 下失效**——单引号被字面传入导致模式不匹配，集成套件未被排除。本机实测 `npm test`：**5 集成套件被错误执行，44 用例失败**（无 DB 报 503/连接错误），exit 非 0。改用双引号后 25 单测全绿 exit 0。
  - 影响：验收标准"`npm test` 无 DB 环境可跑（25 单测全绿）"**在 Windows 开发机上不成立**（Linux/macOS CI 的 sh 引号语义下可能正常）。
  - 建议修复：`package.json` 脚本中的单引号改为 `\"` 转义双引号，或将 `testPathIgnorePatterns` 移入 `jest.config.js`（与 checklist 原始描述一致）。

### 3.3 BK-R-015 体积压缩 —— 部分落实（形式偏差 + 验收待办）
- **落实部分**：CI `build.yaml:188,191` 已含 `--split-per-abi --obfuscate --split-debug-info=build/symbols` 与 appbundle；`isMinifyEnabled/isShrinkResources=true`（`build.gradle.kts:66-71`）；符号表归档 `:201`；占位依赖审计注释（`pubspec.yaml:17-20`）。
- **偏差**：Checklist 声称"release abiFilters 移除 x86_64（build.gradle.kts ndk abiFilters）"，实际 `build.gradle.kts:89-93` **无 abiFilters**（注释说明禁用，改由 CI `--target-platform android-arm,android-arm64` 实现）。效果等同，但与文档字面不符。
- **验收待办（文档自认）**：arm64 APK ≤ 28MB 待 CI 实测。

### 3.4 验收报告未回填 —— 流程项未执行
- Spec §5.6 与 07 文档状态规则 5 均要求"每波次完成后回填《04-验收报告.md》审计记录"。实测 `docs/wbspecs/04-验收报告.md` **无 MR-1~MR-4 任何审计记录**（`docs/04-验收报告.md` 不存在，实际文件在 wbspecs 子目录）。文档链完整性未闭环。

### 3.5 文档自认的设备/真机验收开放项（非偏差，但属"未完成验收"）
| 任务 | 待办验收 |
|---|---|
| BK-R-007 | adb pull hexdump 验证明文头消除 |
| BK-R-008 | 双设备同账号互同步 e2e |
| BK-R-011 | 被邀请账号共享账本 e2e |
| BK-R-016 | targetSdk 36 edge-to-edge 键盘遮挡走查 |
| BK-R-017 | 深色模式全页面真机走查 |
| BK-R-020 | 读屏器全流程走查 |

### 3.6 测试数量口径存疑（无法对齐）
- 07 文档各任务声称"331/339/346/349/352/353/357 全量测试绿"，终态为 **357**。
- 实测统计 `app/test/` 下 58 个 `*_test.dart` 文件、`test(` 调用约 **288** 次，与 357 无法对齐（口径不明：可能含 testWidgets/分组计数差异）。
- 且文档声称的"flutter analyze 0 issues"与"app 全量测试绿"**本机无法复跑验证**（无 Flutter SDK），建议以 CI 产物为准补充证据。

---

## 4. 与文档要求不符的具体说明汇总

| # | 位置 | 文档声称 | 实际情况 | 性质 |
|---|---|---|---|---|
| 1 | `constants.dart:10` | "删除 kDefaultBookId 回退"（R-004 [x]） | 常量保留，仅生产回退逻辑删除 | 轻微偏差 |
| 2 | `server/package.json:10,15` | "去除 --coverage=false"（R-021 [x]） | 仍保留；覆盖经 test:coverage 生效 | 形式偏差 |
| 3 | `server/package.json:10` | "npm test 无 DB 可跑" | Windows 下单引号正则失效，集成套件泄漏执行致 44 失败 | **实质缺陷** |
| 4 | `build.gradle.kts:89-93` | "release abiFilters 移除 x86_64" | 无 abiFilters，改由 CI --target-platform 实现 | 形式偏差 |
| 5 | `wbspecs/04-验收报告.md` | "每波次回填审计记录"（Spec §5.6） | 无 MR-1~MR-4 记录 | 流程未执行 |
| 6 | 07 文档各任务 | "357 全量测试绿 / analyze 0 issues" | 本机无法复跑；测试计数口径对不齐 | 证据不足 |

---

## 5. 建议

1. **优先修复 §3.2 偏差 2**：`npm test` Windows 兼容性（改双引号或迁入 jest.config.js），属真实可复现缺陷。
2. 回填 `wbspecs/04-验收报告.md` 的 MR-1~MR-4 审计记录，闭合文档链。
3. 修正文档中 3 处过度声明（kDefaultBookId"删除"、--coverage=false"去除"、abiFilters），或补齐代码使其字面一致。
4. 在 CI 上沉淀 flutter analyze / flutter test / APK 体积实测证据，替代文档中无法复现的计数声明。
5. 设备级验收项（§3.5）建议在真机到位后按 Spec §5.3/§5.4 清单逐项销号。
