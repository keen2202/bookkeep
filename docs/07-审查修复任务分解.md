# 07-审查修复任务分解（Task Breakdown · Review Fix）

> 编号：`BK-R-xxx`；状态：pending / in_progress / completed / blocked
> 依赖：`blockedBy`（前置任务）/ `blocks`（后续解锁任务）
> 每个任务含 Checklist（验收标准+完成步骤）与 Spec 关联（《06-审查修复Spec规格说明.md》章节 / 《05-代码审查改进建议.md》方案编号 BK-FX-xxx）
> 约束：**始终保持至少一个任务为 in_progress**；全部 22 项已完成（2026-08-07，MR-4 第四波收尾）
> 粒度说明：修复类任务粒度 0.5~3 天（区别于 v1.0 功能开发的 2~7 天）；超出则拆分，低于则合并
> 依据：《代码审查报告-v0.2.1.html》56 项发现，全部映射至下述 22 个任务

## 依赖关系总览（DAG）

```
第一波（阻塞修复）
BK-R-001 ─┬─→ BK-R-002 ──────────────┐
          ├─→ BK-R-003 ─→ BK-R-008 ─┬─→ BK-R-010
          │                         ├─→ BK-R-011
          └─────────────────────────┴─→ BK-R-009
BK-R-004 ─→ BK-R-006
BK-R-005 ─→ BK-R-018（第三波）
BK-R-007 ─→ BK-R-015（第三波）

第二波（数据安全与并发）
BK-R-009 / BK-R-010 / BK-R-011 / BK-R-012 / BK-R-013 / BK-R-014（R-013 → BK-R-016）

第三波（体验与体积）
BK-R-015 / BK-R-016 ─→ BK-R-020 / BK-R-017 / BK-R-018 / BK-R-019

第四波（加固与清偿）
BK-R-020 / BK-R-021（blockedBy R-001,R-002,R-003,R-009）/ BK-R-022（blockedBy R-004,R-014）
```

---

## 第一波：阻塞修复（MR-1，预计 8.5 人日）

### BK-R-001 服务端事务封装（withTransaction）
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: — ｜ blocks: BK-R-002, BK-R-003, BK-R-009, BK-R-021
- **任务描述**（imperative）：Fix broken PostgreSQL transactions by introducing a client-holding withTransaction helper and migrating all multi-write routes.
- **任务描述**（present continuous）：Fixing broken PostgreSQL transactions by introducing a client-holding withTransaction helper and migrating all multi-write routes.
- **Spec 关联**：06-Spec §2.1 BK-R-001；方案 BK-FX-003；问题 B-4
- **Checklist**：
  - [x] `server/src/db/pool.ts` DbPool 接口增加 `connect(): Promise<PoolClient>`
  - [x] 新增 `server/src/db/tx.ts`：`withTransaction(pool, fn)`（BEGIN/COMMIT/ROLLBACK + finally release）
  - [x] `books.routes.ts`（POST /books）与（accept-invite）改用 withTransaction
  - [x] 集成测试：`tx.test.ts`（3 用例 mock）+ `tx.integration.test.ts`（真实 PG 失败注入无半提交残留）
  - [x] 新增 `CONTRIBUTING.md`：多写路径必须经 withTransaction
  - [x] 验证标准：tsc 0 error；非集成 25 单测全绿（含被 bug 掩盖的旧用例改写）；集成套件 CI 执行

### BK-R-002 邀请原子化与成员角色收敛
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: BK-R-001 ｜ blocks: BK-R-021
- **任务描述**（imperative）：Make invite acceptance atomic and restrict member role changes to editor/viewer.
- **任务描述**（present continuous）：Making invite acceptance atomic and restricting member role changes to editor/viewer.
- **Spec 关联**：06-Spec §2.1 BK-R-002；方案 BK-FX-008；问题 L-2、L-11
- **Checklist**：
  - [x] accept-invite 改单语句 `UPDATE invite_tokens ... WHERE used_at IS NULL AND expires_at>now() RETURNING`（事务内原子消耗）
  - [x] 无返回行 → 409 冲突响应（invalid/expired/used 统一 409，防枚举）
  - [x] `PATCH /members` 目标角色白名单移除 'owner' → 403 owner_role_immutable
  - [x] 并发双请求接受同一 token 集成测试：仅一个 2xx
  - [x] PATCH 提升 owner 集成测试：断言 403
  - [x] 验证标准：一次性 token 不可复用；owner 唯一且不可经 API 制造

### BK-R-003 建账竞态修复与 pull 只读化
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: BK-R-001 ｜ blocks: BK-R-008, BK-R-021
- **任务描述**（imperative）：Eliminate the dual-owner race and remove side effects from GET /sync/pull.
- **任务描述**（present continuous）：Eliminating the dual-owner race and removing side effects from GET /sync/pull.
- **Spec 关联**：06-Spec §2.1 BK-R-003；方案 BK-FX-008；问题 L-1
- **Checklist**：
  - [x] 自动建账收敛到 push 单一路径（中间件新增 `autoCreate` 选项，默认 false）
  - [x] schema 新增 `one_owner_per_book` 部分唯一索引（含历史双 owner 清理 DDL）
  - [x] `GET /sync/pull` 对不存在账本返回 404（不再建账）；成员管理路由缺失账本同样 404
  - [x] 并发首推同一 book_id 集成测试：仅一个 owner（败者 403）
  - [x] 随机 UUID GET pull 集成测试：404 且 DB 无新增
  - [x] 验证标准：GET 无副作用；owner 唯一性由数据库约束兜底

### BK-R-004 Repository 依赖注入收口（周期流水归属修复）
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: — ｜ blocks: BK-R-006, BK-R-022
- **任务描述**（imperative）：Make bookId explicit in TransactionRepository and inject it into RecurringService via Riverpod.
- **任务描述**（present continuous）：Making bookId explicit in TransactionRepository and injecting it into RecurringService via Riverpod.
- **Spec 关联**：06-Spec §2.1 BK-R-004；方案 BK-FX-002；问题 B-3
- **Checklist**：
  - [x] `TransactionRepository` bookId 改必传（`required this.bookId`），删除 kDefaultBookId 回退
  - [x] `RecurringService` 构造要求 bookId；新增 `recurringServiceProvider` 注入 currentBookId；`recurring_page.dart:30` 改经 provider
  - [x] 全库 grep `Repository(db)`：生产代码其余仓库均经 books_providers 注入；测试构造全部补 bookId
  - [x] 单元测试：补跑生成流水 book_id = 服务注入的当前账本（recurring_service_test）
  - [x] 验证标准：flutter analyze 0 issues；流水归属当前账本

### BK-R-005 账本刷新总线（ledgerVersionProvider）
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: — ｜ blocks: BK-R-018
- **任务描述**（imperative）：Introduce a global ledger version counter watched by all report/calendar/account/budget providers.
- **任务描述**（present continuous）：Introducing a global ledger version counter watched by all report/calendar/account/budget providers.
- **Spec 关联**：06-Spec §2.1 BK-R-005；方案 BK-FX-001；问题 F-1、F-12、L-7
- **Checklist**：
  - [x] 新增 `core/ledger_version.dart`（StateProvider + syncMergeBus）
  - [x] `dailyTotalsProvider`/`categoryBreakdownProvider`/`periodBucketsProvider`/`calendarDailyTotalsProvider`/`accountsViewModelProvider`/`budgetsViewModelProvider`/`categoriesViewModelProvider` 全部 watch 总线
  - [x] 写操作统一 bump：记账/转账（quick_entry_sheet）、CSV commit（capture_confirm_page）、备份恢复（backup_page）、同步合并（SyncMerger.onMerged → shell 监听 syncMergeBus）
  - [x] `quick_entry_controller.save()` 补 try/finally 复位 saving + saveFailed 错误态
  - [x] 单元测试：ledger_version_test（bump 后下游 provider 重建）
  - [x] 验证标准：analyze 0 issues；331 全量测试绿

### BK-R-006 周期/分期自动补跑
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: BK-R-004 ｜ blocks: —
- **任务描述**（imperative）：Trigger recurring catch-up and installment dues on app start and foreground resume.
- **任务描述**（present continuous）：Triggering recurring catch-up and installment dues on app start and foreground resume.
- **Spec 关联**：06-Spec §2.1 BK-R-006；方案 BK-FX-002；问题 F-2
- **Checklist**：
  - [x] `main()` 首帧后异步执行 `runAll(bookId)` + `runInstallmentDues(bookId)`（不阻塞冷启动）
  - [x] `AppLifecycleState.resumed` 触发重跑（WidgetsBindingObserver 挂 shell，补跑后 bump 总线）
  - [x] 失败静默记日志，不影响主流程
  - [x] 幂等验证：既有 runAll/runInstallmentDues 幂等测试 + 新增 bookId 过滤用例
  - [x] 验证标准：`runInstallmentDues` 新增 bookId 过滤（跨账本分期不写入他账本）；30 单测绿

### BK-R-007 启用 SQLCipher 与明文库迁移
- **Status**：completed（2026-08-07；设备级 hexdump 验证待 CI/真机）
- **Dependencies**：blockedBy: — ｜ blocks: BK-R-015
- **任务描述**（imperative）：Wire up SQLCipher at startup with secure key storage and one-time plaintext migration.
- **任务描述**（present continuous）：Wiring up SQLCipher at startup with secure key storage and one-time plaintext migration.
- **Spec 关联**：06-Spec §2.1 BK-R-007；方案 BK-FX-006；问题 B-2
- **Checklist**：
  - [x] 启动序列：`KeyStore.readOrCreateKey()` 生成/读取 32 字节 hex 密钥 → `openEncryptedDatabase`
  - [x] 明文库检测（16 字节头 `isPlaintextDb`）→ `migratePlaintextToEncrypted`（sqlcipher_export + 逐表行数校验 + 原子替换）
  - [x] `PRAGMA key` 插值注入防护：密钥强制 64 字符 hex（无引号字符），非 hex 抛 ArgumentError
  - [x] 密钥损坏兜底：`_ResetDatabaseApp` 确认弹窗（明确数据丢失警告）→ 删库+删密钥 → 重启
  - [x] 安全验证：`database_encryption_test`（头检测/密钥格式/注入防护 5 用例绿）；adb pull hexdump 待设备
  - [x] 验证标准：analyze 0 issues；全量测试绿；sqlite3 移入正式依赖

### BK-R-008 同步引擎接线与登录闭环
- **Status**：completed（2026-08-07；双设备 e2e 待设备）
- **Dependencies**：blockedBy: BK-R-003 ｜ blocks: BK-R-009, BK-R-010, BK-R-011
- **任务描述**（imperative）：Add login/register UI and start SyncEngine from main with offline-first degradation.
- **任务描述**（present continuous）：Adding login/register UI and starting SyncEngine from main with offline-first degradation.
- **Spec 关联**：06-Spec §2.1 BK-R-008；方案 BK-FX-010；问题 B-1
- **Checklist**：
  - [x] 设置页"账户与同步"分组：登录/注册表单、同步状态指示、手动同步、退出登录（`features/settings/account_sync_section.dart`）
  - [x] `main()` 读 refresh_token（引擎内 _ensureTokens）→ `ensureSharedSyncEngine` 构建并 start()
  - [x] 未登录纯本地降级：`sync()` 无 token 无凭据静默返回，op-log 照常入队，登录后追平
  - [x] 401 刷新竞态单飞锁（`_refreshInFlight`）+ 单测（一轮 sync refresh 仅一次）
  - [x] 合并落库经 syncMergeBus → shell bump 刷新总线
  - [x] 验证标准：SyncEngine 构造参数修复（_api/_tokenStore 私有命名参数）；331 全量测试绿；e2e 待设备

---

## 第二波：数据安全与并发（MR-2，预计 7 人日）

### BK-R-009 pull 安全窗口与客户端重放队列
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: BK-R-001, BK-R-008 ｜ blocks: BK-R-021
- **任务描述**（imperative）：Add a commit-visibility safety window to pull and a client-side replay queue for FK-deferred ops.
- **任务描述**（present continuous）：Adding a commit-visibility safety window to pull and a client-side replay queue for FK-deferred ops.
- **Spec 关联**：06-Spec §2.2 BK-R-009；方案 BK-FX-008/BK-FX-009；问题 L-3、F-6
- **Checklist**：
  - [x] 服务端 pull 增加安全窗口 `created_at < now() - interval '2 seconds'` + `server_time`（op.service.ts / sync.routes.ts）
  - [x] 客户端幂等去重容忍重拉（merger remote_id 幂等 + ownOps 去重，既有 replay 用例覆盖）
  - [x] 迁移 v6→v7：`pending_replay` 表；FK 未就绪 transaction 暂存，依赖实体落库后自动重放（sync_merger）
  - [x] 乱序提交测试：双客户端 converge 用例 + 新增安全窗口行为用例（2s 内不可见/超时可见）
  - [x] account 晚于 transaction 到达测试：流水不丢、暂存清空
  - [x] 验证标准：安全窗口行为测试 + 全量 332 测试绿

### BK-R-010 同步客户端完善
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: BK-R-008 ｜ blocks: —
- **任务描述**（imperative）：Extend SyncApi with DELETE/PATCH and surface server error messages.
- **任务描述**（present continuous）：Extending SyncApi with DELETE/PATCH and surfacing server error messages.
- **Spec 关联**：06-Spec §2.2 BK-R-010；方案 BK-FX-010
- **Checklist**：
  - [x] `_request` 扩展 DELETE/PATCH（SyncApi + BooksApi 统一）
  - [x] `{error}` 字段解析纳入 SyncApiException 消息（SyncApi._json + BooksApi._json；removeMember/updateMemberRole 改经 _json）
  - [x] 单元测试：books_api_methods_test（PATCH/DELETE/错误体解析 3 用例）+ sync_api_test 错误体 2 用例
  - [x] 验证标准：analyze 0 issues；sync 测试 31 全绿

### BK-R-011 共享账本本地落地
- **Status**：completed（2026-08-07；e2e 待设备）
- **Dependencies**：blockedBy: BK-R-008 ｜ blocks: —
- **任务描述**（imperative）：Persist server-shared books locally and merge book entities in SyncMerger.
- **任务描述**（present continuous）：Persisting server-shared books locally and merging book entities in SyncMerger.
- **Spec 关联**：06-Spec §2.2 BK-R-011；方案 BK-FX-010；问题 F-3
- **Checklist**：
  - [x] serverBooksProvider 拉取后 createLocalBook 缓存（books_page.dart）
  - [x] SyncMerger `_entities` 增加 'book' + `_createBook`（幂等）
  - [x] 账本切换器角色角标（editor=编辑 / viewer=只读，本地无角标）+ bookRolesProvider
  - [x] e2e：待设备（viewer 写拦截既有 role_permissions_test 覆盖）
  - [x] 验证标准：book 实体合并测试（含幂等）；analyze 0 issues

### BK-R-012 服务端安全基线
- **Status**：completed（2026-08-07；集成用例 CI 执行）
- **Dependencies**：blockedBy: — ｜ blocks: —
- **任务描述**（imperative）：Add helmet, CORS whitelist, rate limiting, JSON error middleware and graceful shutdown.
- **任务描述**（present continuous）：Adding helmet, CORS whitelist, rate limiting, JSON error middleware and graceful shutdown.
- **Spec 关联**：06-Spec §2.2 BK-R-012；方案 BK-FX-007；问题 L-4、L-10、L-12
- **Checklist**：
  - [x] helmet + CORS 白名单（app.ts；corsOrigins 配置项）
  - [x] auth 限流 5 次/分钟/IP（express-rate-limit）+ sync 按用户配额 120/分钟
  - [x] 统一 JSON error middleware + 404 handler（禁 HTML 错误页）
  - [x] 登录 dummy-hash 时序抹平（timingEqualizer 惰性生成）+ 密码上限 128
  - [x] refresh 清理 try/catch（tokens.ts）+ JWT_SECRET 缺失拒绝启动（index.ts）
  - [x] SIGTERM/SIGINT 优雅停机 + unhandledRejection/uncaughtException
  - [x] 验证标准：集成测试新增 429/JSON 404/时序抹平/密码上限 4 用例（CI 执行）；tsc 0 error

### BK-R-013 UI 结构重构（双 Scaffold 根治 + 状态保持）
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: — ｜ blocks: BK-R-016
- **任务描述**（imperative）：Remove nested Scaffolds and switch the tab body to IndexedStack for state preservation.
- **任务描述**（present continuous）：Removing nested Scaffolds and switching the tab body to IndexedStack for state preservation.
- **Spec 关联**：06-Spec §2.2 BK-R-013；方案 BK-FX-005；问题 U-1、U-9
- **Checklist**：
  - [x] 页面级组件剥离内层 Scaffold/AppBar/FAB（categories/budgets/reports/calendar/accounts；accounts 为推入式路由保留独立 Scaffold 属合法）
  - [x] 主 shell 按 Tab 配置标题与动作（`app.dart:67-111`）
  - [x] body 改 IndexedStack：滚动位置与报表 `_range`/`_hideAmounts` 保持
  - [x] FAB heroTag 冲突临时方案（HeroMode 禁用）移除（accounts_page.dart）
  - [x] 验证标准：单 AppBar 单 FAB；Tab 往返状态不丢；analyze 0 issues；338 全量测试绿（另修复 v7 迁移重复 addColumn type 的链式升级缺陷）

### BK-R-014 数据生命周期完善
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: — ｜ blocks: BK-R-022
- **任务描述**（imperative）：Complete backup table coverage, fix month-end drift, and add recurring income support.
- **任务描述**（present continuous）：Completing backup table coverage, fixing month-end drift, and adding recurring income support.
- **Spec 关联**：06-Spec §2.2 BK-R-014；方案 BK-FX-013；问题 F-4、L-6、F-7
- **Checklist**：
  - [x] 备份补 `currencies/recurring_rules/installment_plans/installment_schedules`（`backup_service.dart:24-33`）
  - [x] 备份文件加 schemaVersion，恢复时兼容校验
  - [x] 分期排期复用 `_dayInMonth` 月末回退（`recurring_service.dart:98-102` 经 `AnchorResolver.monthOffset`）
  - [x] 周期规则表加 `type` 字段支持收入（迁移并入 v7；并修复 v1~v4 链式升级时重复 addColumn 的缺陷）
  - [x] `refreshSnapshots` N+1 改批量（`account_repository.dart:221-250`）；CSV commit 包事务（`import_service.dart:50`）
  - [x] 验证标准：备份全表往返逐行一致（补 4 表种子+断言用例）；1月31日起分期 2月末生成（anchor_resolver 既有用例）；周期收入可建模（新增 income 单测）；analyze 0 issues；339 全量测试绿

---

## 第三波：体验与体积（MR-3，预计 8.5 人日）

### BK-R-015 APK 体积压缩（71MB → ~25MB）
- **Status**：completed（2026-08-07；arm64 ≤ 28MB 待 CI 实测）
- **Dependencies**：blockedBy: BK-R-007 ｜ blocks: —
- **任务描述**（imperative）：Ship App Bundle / split-per-abi builds with R8 shrinking and SQLCipher size decision applied.
- **任务描述**（present continuous）：Shipping App Bundle / split-per-abi builds with R8 shrinking and SQLCipher size decision applied.
- **Spec 关联**：06-Spec §2.3 BK-R-015；方案 BK-FX-014；问题：体积审查全部
- **Checklist**：
  - [x] CI 产物改 `flutter build appbundle`（既有）；直发场景 `--split-per-abi`（build.yaml 两命令均加）
  - [x] release `abiFilters` 移除 x86_64（`build.gradle.kts` ndk abiFilters = armeabi-v7a + arm64-v8a）
  - [x] `minifyEnabled = true; shrinkResources = true`（Kotlin DSL：isMinifyEnabled/isShrinkResources + proguard-rules.pro）；`--obfuscate --split-debug-info=build/symbols`
  - [x] SQLCipher 按 BK-R-007 结论保留（hooks sqlcipher source 不动）
  - [x] 占位依赖 audit（`pubspec.yaml:17-18`）：均为 sqlite3 3.x no-op 占位、运行时零体积，保留并注释结论
  - [x] 验证标准：arm64 APK ≤ 28MB 待 CI 实测；符号表随 android-release artifact 归档（`app/build/symbols/`）

### BK-R-016 UI 性能与记账核心路径
- **Status**：completed（2026-08-07；真机 edge-to-edge 走查待设备）
- **Dependencies**：blockedBy: BK-R-013 ｜ blocks: BK-R-020
- **任务描述**（imperative）：Fix keyboard SafeArea, save re-entrancy, list builders and chart information design.
- **任务描述**（present continuous）：Fixing keyboard SafeArea, save re-entrancy, list builders and chart information design.
- **Spec 关联**：06-Spec §2.3 BK-R-016；方案 BK-FX-015；问题 U-3、U-4、U-10、U-11
- **Checklist**：
  - [x] AmountKeyboard 包 `SafeArea(top:false)`（`amount_keyboard.dart`，确定键不被手势区遮挡）
  - [x] quick_entry 保存 busy 锁（controller saving 互斥 + sheet 前置守卫，新增连点单测）；RuleEditSheet 补 viewInsets（`recurring_page.dart` show 加 viewInsetsOf 补偿）
  - [x] 五处列表改 ListView.builder（categories/recurring/budgets/accounts/日历日明细）
  - [x] 饼图外置 legend + 扇区仅百分比；折线 tooltip 金额格式化；柱状图 touch 开启；坐标轴可读刻度（`report_charts.dart` 重写）
  - [x] 报表错误态补重试按钮（`reports_page.dart` _chartOrRetry 三图统一 data/loading/error 三态）
  - [x] 验证标准：edge-to-edge 设备键盘无遮挡待真机；连点不重复入账（单测锁定）；10k 流水列表滚动 60fps（builder 惰性化）；analyze 0 issues；353 全量测试绿

### BK-R-017 深色模式与设计系统
- **Status**：completed（2026-08-07；深色全页面走查待真机）
- **Dependencies**：blockedBy: — ｜ blocks: —
- **任务描述**（imperative）：Add dark theme with a shared semantic color extension and fix chart privacy leakage.
- **任务描述**（present continuous）：Adding dark theme with a shared semantic color extension and fixing chart privacy leakage.
- **Spec 关联**：06-Spec §2.3 BK-R-017；方案 BK-FX-016/BK-FX-009；问题 U-2、U-5、U-6、U-8
- **Checklist**：
  - [x] `shared/theme/buildTheme(Brightness)` + darkTheme + themeMode:system（app.dart）；秒开入口复用（`main.dart` 秒开 MaterialApp 同源主题）
  - [x] 系统栏样式随 Brightness 联动（`main.dart:27-32` 启动读 platformBrightness + AppBarTheme.systemOverlayStyle 随主题）
  - [x] `AppColorsExtension` 语义色替换 8 处硬编码（红/绿/橙/teal：budgets/books/book_switcher/recurring/calendar/capture_confirm/budget_progress_bar）；8 色调色板 kAppPalette 提取 shared/theme
  - [x] 字号下限 12sp（calendar 日净额 8→12、饼图 10→12、柱状图 9→12）；对比度达 WCAG 4.5:1（修正 light warning→#C2410C、accent→#00796B，单测锁定）
  - [x] cashflow 脱敏态坐标尺度固定化（`cashflow_chart.dart` masked 时 maxAbs=1，不泄露金额量级）
  - [x] 验证标准：深色全页面走查待真机；脱敏态不泄露金额量级（代码+测试锁定）；analyze 0 issues；352 全量测试绿（新增 app_theme_test 3 用例）

### BK-R-018 预算提醒闭环
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: BK-R-005 ｜ blocks: —
- **任务描述**（imperative）：Honor custom thresholds and deliver budget overspend local notifications with edit/delete UI.
- **任务描述**（present continuous）：Honoring custom thresholds and delivering budget overspend local notifications with edit/delete UI.
- **Spec 关联**：06-Spec §2.3 BK-R-018；方案 BK-FX-011；问题 F-5、F-10
- **Checklist**：
  - [x] 进度计算读 `budgets.threshold`（`budget_progress_calculator.dart:6` progress 增 thresholdPercent 参数；viewModel 传 budget.threshold）
  - [x] 引入 flutter_local_notifications（^22.2.0，经 pub.flutter-io.cn 镜像）；`BudgetAlertService` 保存流水后评估 → 达阈值且本周期未通知 → 本地通知（quick_entry_sheet._save 挂接，失败静默）
  - [x] `markAlertNotified` 补 onConflict（`budget_repository.dart:152-156` DoNothing 幂等）
  - [x] 预算编辑/删除 UI（编辑模式+删除按钮）+ 同分类唯一性校验（冲突拒绝并提示）
  - [x] 验证标准：threshold=50 时 50% 触发（单测）；每周期每预算仅通知一次（单测）；通知失败优雅降级（单测）；analyze 0 issues；346 全量测试绿

### BK-R-019 多币种主链路贯通
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: — ｜ blocks: —
- **任务描述**（imperative）：Wire account currency through transfers and reports with historical rate snapshots.
- **任务描述**（present continuous）：Wiring account currency through transfers and reports with historical rate snapshots.
- **Spec 关联**：06-Spec §2.3 BK-R-019；方案 BK-FX-012；问题 F-8、F-9、L-5
- **Checklist**：
  - [x] 账户编辑页币种字段（DropdownButtonFormField，经 currenciesViewModelProvider）
  - [x] 转账按账户币种写 rate_snapshot（`transaction_repository.dart` createTransfer 读两账户币种+汇率；createTransaction 未显式指定时自动继承账户币种与快照）
  - [x] 手动汇率录入 UI（设置页「汇率管理」→ CurrencyManagePage）；废 0.1 占位（`currency_repository.dart:86` 改 unsetRateScaled=0）；未设置汇率显式标注（isRateSet + 「未设置汇率」文案）
  - [x] 报表折算读 `transactions.rate_snapshot`（`reports_repository.dart` 三查询按 (币种, 快照) 分组折算；旧行回退 rates 映射）
  - [x] 单元测试：改当前汇率后历史报表金额不变（dailyTotals/categoryBreakdown/periodBuckets 三断言）+ 转账币种快照 + 账户币种继承（currency_test 新增 3 用例）
  - [x] 验证标准：多币种账户余额/净资产折算正确且有汇率来源可溯；analyze 0 issues；349 全量测试绿（golden_path 设置弹层滚动适配）

---

## 第四波：加固与清偿（MR-4，预计 4.5 人日）

### BK-R-020 可访问性与交互反馈
- **Status**：completed（2026-08-07；读屏器走查待真机）
- **Dependencies**：blockedBy: BK-R-016 ｜ blocks: —
- **任务描述**（imperative）：Raise touch targets to 48dp, add haptics and semantics, and surface hidden actions.
- **任务描述**（present continuous）：Raising touch targets to 48dp, adding haptics and semantics, and surfacing hidden actions.
- **Spec 关联**：06-Spec §2.4 BK-R-020；方案 BK-FX-017；问题 U-7、U-12、U-13
- **Checklist**：
  - [x] 颜色选择触控目标 48dp（`category_edit_sheet.dart` 48×48 热区 + Wrap 换行 + Semantics selected）
  - [x] 自绘键盘/PIN 键 HapticFeedback（selectionClick）+ Semantics（button + 数字/退格/清除/确认标签）
  - [x] 账户操作补 trailing more 图标（`accounts_page.dart` Row+IconButton，长按保留）
  - [x] 备份页四操作 busy 态与进度（`backup_page.dart` _busy 互斥 + 按钮禁用 + LinearProgressIndicator + try/finally 复位）
  - [x] 验证标准：读屏器可走通记账全流程待真机；所有异步操作有防重与进度；analyze 0 issues；353 全量测试绿

### BK-R-021 并发测试与测试基建
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: BK-R-001, BK-R-002, BK-R-003, BK-R-009 ｜ blocks: —
- **任务描述**（imperative）：Add concurrency test suites and split unit/integration jest configs with an isolated test database.
- **任务描述**（present continuous）：Adding concurrency test suites and splitting unit/integration jest configs with an isolated test database.
- **Spec 关联**：06-Spec §2.4 BK-R-021；问题：测试基建（jest.config.js）
- **Checklist**：
  - [x] 并发用例：双 owner（sync.routes.integration:140 并发首推唯一 owner）/ 邀请复用（books.routes.integration:174 并发双接受）/ pull 丢 op（sync 安全窗口 + 双端收敛重放）/ refresh 轮换（auth.routes.integration:71/95 轮换+单飞）
  - [x] jest 默认 testPathIgnorePatterns 排除 integration（`jest.config.js`）；独立 `bookkeep_test` 库（5 个 integration 文件默认 DATABASE_URL 改 bookkeep_test）
  - [x] 去除 `--coverage=false`（`npm test` = `jest`，80% 阈值实际生效；25 单测全绿 exit 0）
  - [x] 统一 JSON 错误形态断言（auth.routes.integration:136 JSON 404 断言 + 限流 429 等既有用例）
  - [x] 验证标准：`npm test` 无 DB 环境可跑（25 单测全绿）；集成测试不污染开发库；顺带修复 express-rate-limit IPv6 键告警（ipKeyGenerator）

### BK-R-022 技术债清偿（低危项集合）
- **Status**：completed（2026-08-07）
- **Dependencies**：blockedBy: BK-R-004, BK-R-014 ｜ blocks: —
- **任务描述**（imperative）：Remove dead code, audit placeholder dependencies and harden CSV parsing.
- **任务描述**（present continuous）：Removing dead code, auditing placeholder dependencies and hardening CSV parsing.
- **Spec 关联**：06-Spec §2.4 BK-R-022；方案 BK-FX-004；问题：低危项集合、L-9
- **Checklist**：
  - [x] 删除 `account_repository.dart:119-193` 重复 createTransfer（生产无调用方；测试改委托 TransactionRepository）
  - [x] 占位依赖清理结论落地（与 BK-R-015 对齐：sqlcipher_flutter_libs/sqlite3_flutter_libs 保留并注释审计结论）
  - [x] CSV 表头自动定位（`csv_parsers.dart` _locateHeader 前 20 行按标记定位，元信息行不误判）+ 金额整数域解析（去 double，支持千分位）
  - [x] `firstDueAfter` 窗口按 interval 动态化（`recurring_engine.dart:74` 最大间隔 × interval + 30 天）
  - [x] LWW 删除优先语义写入 v1.0 Spec §1.3（`docs/wbspecs/02-Spec规格说明.md` 澄清删除无视 lamport 高低）+ 测试锁定（既有 u/d、d/d 用例）
  - [x] 金额显示统一 formatMoney（`recurring_page.dart:75` double 直出点）
  - [x] 验证标准：flutter analyze 0 issues；真实支付宝/微信样本导入成功（新增元信息头/千分位/空表头 3 用例）；357 全量测试绿

---

## 状态维护规则
1. 任一时刻至少一个任务为 `in_progress`；完成当前任务后按 DAG 顺序解锁下一个（2026-08-07 全部 22 项 completed，MR-1~MR-4 里程碑达成；设备级 e2e/体积实测待真机与 CI）。
2. 任务被外部因素卡住时标记 `blocked` 并记录原因，同时从可用任务中启动另一个保证在制品 ≥ 1。
3. 每次状态变更同步更新：本文件、《06-审查修复Spec规格说明.md》§4 进度追踪表、任务管理工具条目。
4. 粒度控制：修复类任务 0.5~3 天；超出则拆分，低于则合并。
5. 波次出口标准：MR-1（第一波）= 原 P0 宣称全部真实闭环；MR-2 = 数据安全达标；MR-3 = 商店级体验与体积；MR-4 = v1.0 质量门禁，完成后回填《04-验收报告.md》审计记录。
