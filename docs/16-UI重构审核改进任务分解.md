# 16 - UI 重构审核改进任务分解（WBS 与依赖）

| 项目 | 内容 |
| --- | --- |
| 文档编号 | BK-DOC-16 |
| 版本 | v1.0 |
| 日期 | 2026-08-12 |
| 状态 | 进行中（BK-A-001~008 completed；BK-A-009 blocked 待真机；BK-A-010 completed） |
| 技术规格 | `docs/15-UI重构审核改进Spec规格说明.md` |
| 审核依据 | `docs/14-UI重构审核报告.md` |
| 上游文档 | `docs/09-UI设计文档-主题与视觉体系.md`、`docs/10-UI重构Spec规格说明.md`、`docs/11-UI重构任务分解.md`、`docs/13-UI重构验收报告.md` |
| 任务编号段 | BK-A-001 ~ BK-A-010 |

---

## 1. 总览

- **范围**：14 审核报告不符合项 #1~#8 的修复（F1~F8），含代码修复、测试补齐、工具链升级、真机验证与文档治理。
- **工时**：6.5 个工作日（单人），粒度 0.5~1 天。
- **排期窗口**：2026-08-12 ~ 2026-08-20（BK-A-009 真机环节视设备可用性浮动，允许独立排期）。
- **节奏约定**：始终维持 ≥ 1 个 in_progress 任务；任务状态变更即时回填本文件；每个波次结束过里程碑评审。
- **状态口径**（沿用 15 文档 A3 治理）：`pending` 待开始 / `in_progress` 进行中 / `completed` 已完成（代码类以"CI 全绿 + 用例覆盖"为准；真机类以"证据归档"为准）/ `blocked` 被阻塞（须在备注中写明阻塞条件）。

## 2. 波次依赖（DAG）

```
W1 阻断修复（P0/P1）                W2 质量补强（P2，彼此独立、与 W1 并行可行）
BK-A-001 ──> BK-A-002 ──┐          BK-A-005（按钮收敛）
                        ├──> BK-A-009（真机验证）──> BK-A-010（文档回填）
BK-A-003 ───────────────┤          BK-A-006（lint 升级）───────────────┘
BK-A-004（迁移测试）────┘          BK-A-007（备份收口）───────────────┘
                                   BK-A-008（秒开背景）───────────────┘
```

- BK-A-001/002/003 是 BK-A-009 的硬前置（缺陷未修真机测试无法通过）。
- W2 四个任务互不依赖，也不阻塞 W1；BK-A-010 需全部代码任务 + 真机验证完成后回填。
- 单人执行建议顺序：001 → 002 → 003 → 004 → 005 → 006 → 007 → 008 → 009 → 010。

## 3. 任务清单

### W1 阻断修复（08-12 ~ 08-14）

---

#### BK-A-001：首次选图 UI 入口

- **任务描述（imperative）**：Add a first-time background image picker entry to the appearance page（为外观页新增首次选图入口，消除未选图态的功能死锁）
- **任务描述（present continuous）**：Adding the first-time background image picker entry（正在新增首次选图入口）
- **Status**：`completed`（2026-08-14，代码 + widget/集成断言齐备）
- **Dependencies**：blockedBy：无；blocks：BK-A-002、BK-A-009
- **对应规范文档**：`docs/15-UI重构审核改进Spec规格说明.md` §4.1（F1）；上游：`docs/10-UI重构Spec规格说明.md` §7、`docs/09-UI设计文档-主题与视觉体系.md` §5.3
- **Checklist**：
  - [x] `appearance_page.dart` `_BackgroundSection` 按三态渲染主操作区：未选图态渲染 `AppButton.primary(block: true)`「选择背景图片」并接通 `_pickAndApply`
  - [x] `_BackgroundPreview` 未选图态渲染占位（surfaceVariant 底 + 图标 + 文案），点击等同「选择背景图片」
  - [x] 未选图态开关保持禁用且提示文案保留
  - [x] `integration_test/background_test.dart` 步骤①后新增 `tap(find.text('选择背景图片'))` 断言链
  - [x] `flutter analyze` 0 issues；`flutter test test/widget/appearance_page_test.dart` 通过
- **涉及文件**：`app/lib/features/settings/appearance_page.dart`、`app/integration_test/background_test.dart`

---

#### BK-A-002：选图 widget 测试真实路径化

- **任务描述（imperative）**：Rewrite the background-picker widget test to drive the real UI interaction path（重写选图 widget 用例为真实 UI 交互路径，移除 controller 直调绕过）
- **任务描述（present continuous）**：Rewriting the background-picker widget test to the real interaction path（正在重写选图用例为真实交互路径）
- **Status**：`completed`（2026-08-14，选图用例改为按钮触发真实交互路径）
- **Dependencies**：blockedBy：BK-A-001；blocks：BK-A-009
- **对应规范文档**：`docs/15-UI重构审核改进Spec规格说明.md` §4.1（R4）；上游：`docs/10-UI重构Spec规格说明.md` §9（Widget 层）
- **Checklist**：
  - [x] `appearance_page_test.dart` 选图用例删除 `tester.runAsync(controller.pickAndApply)` 直调
  - [x] 用例改为 `tester.tap(find.text('选择背景图片'))` 触发，fake `BackgroundService` 注入保持不变
  - [x] 异步断言段落在 `runAsync` 内 pump 的既有隔离模式保留，但触发点必须是按钮
  - [x] 全文件 grep 确认无 `backgroundControllerProvider.notifier` 直调残留
  - [x] 既有 7 个用例 + 改造用例全部通过
- **涉及文件**：`app/test/widget/appearance_page_test.dart`

---

#### BK-A-003：背景图读取绝对路径化

- **任务描述（imperative）**：Resolve the background image to an absolute path via a shared provider before rendering（渲染前经共享 provider 将背景图解析为绝对路径）
- **任务描述（present continuous）**：Resolving the background image path through a shared provider（正在改造背景图路径解析）
- **Status**：`completed`（2026-08-14，`backgroundImageFileProvider` 落地 + 解析用例通过）
- **Dependencies**：blockedBy：无；blocks：BK-A-009
- **对应规范文档**：`docs/15-UI重构审核改进Spec规格说明.md` §4.2（F2/R1）；上游：`docs/10-UI重构Spec规格说明.md` §5.4
- **Checklist**：
  - [x] `background_controller.dart` 新增 `backgroundImageFileProvider`（FutureProvider\<File?\>），内部经 `resolveImageFile()` 绝对路径化
  - [x] `AppBackground.build` 与 `_BackgroundPreview` 改 watch 该 provider，`null` 走无背景分支
  - [x] `pickAndApply()`/`clear()` 同步 invalidate 该 provider（注：provider watch controller 自动随 settings 更新，controller 内显式 invalidate 会触发 Riverpod 依赖环，已在代码注释说明）
  - [x] 全工程 grep 确认渲染链路无相对路径 `File(imagePath)` 残留
  - [x] `app_background_test.dart` 新增"相对路径键值解析为绝对路径且文件存在"用例；既有 5 用例保持通过
- **涉及文件**：`app/lib/shared/theme/background/background_controller.dart`、`app/lib/shared/theme/background/app_background.dart`、`app/lib/features/settings/appearance_page.dart`、`app/test/widget/app_background_test.dart`

---

#### BK-A-004：ThemeSettings 兼容迁移测试补齐

- **任务描述（imperative）**：Add unit tests for legacy-key migration and presetId round-trip of ThemeSettings（补齐 ThemeSettings 旧键迁移与 presetId 往返单测）
- **任务描述（present continuous）**：Adding ThemeSettings legacy-migration unit tests（正在补齐兼容迁移单测）
- **Status**：`completed`（2026-08-14，3 个新用例全绿）
- **Dependencies**：blockedBy：无；blocks：BK-A-010（测试证据归引用）
- **对应规范文档**：`docs/15-UI重构审核改进Spec规格说明.md` §4.3（F3）；上游：`docs/10-UI重构Spec规格说明.md` §2.2 兼容规则、§9 单元层
- **Checklist**：
  - [x] 新用例：仅写 `theme_seed`/`theme_mode` 旧键 → 读出 `presetId == 'custom'` 且 seedColor/mode 按旧键还原
  - [x] 新用例：presetId 读写往返（`applyPreset('t3')` 持久化后重读一致）
  - [x] 新用例：`theme_preset_id` 非法值（'t99'）→ `isCustom == true` 脏数据兜底
  - [x] `flutter test test/unit/features/theme/theme_settings_test.dart` 全绿
- **涉及文件**：`app/test/unit/features/theme/theme_settings_test.dart`

---

### W2 质量补强（08-15 ~ 08-18，彼此独立）

---

#### BK-A-005：原生按钮收敛至 AppButton

- **任务描述（imperative）**：Replace residual FilledButton/OutlinedButton usages with AppButton variants across five files（将 5 个文件 9 处残留原生按钮收敛至 AppButton 四变体）
- **任务描述（present continuous）**：Replacing residual native buttons with AppButton variants（正在收敛残留原生按钮）
- **Status**：`completed`（2026-08-14，lib/features + lib/shared 全量零残留）
- **Dependencies**：blockedBy：无；blocks：BK-A-010
- **对应规范文档**：`docs/15-UI重构审核改进Spec规格说明.md` §4.4（F4/R3，含逐文件变体映射表）；上游：`docs/10-UI重构Spec规格说明.md` §6、`docs/09-UI设计文档-主题与视觉体系.md` §3.4
- **Checklist**：
  - [x] `budget_edit_sheet.dart`（130/211/217）、`backup_page.dart`（145/161/177/202）、`books_page.dart`（117）、`account_sync_section.dart`（140，接入 `loading:` 防重入）、`recurring_page.dart`（321）逐处按映射表替换
  - [x] 外观页自定义 sheet 的 `OutlinedButton.icon`（自定义颜色入口）一并收敛
  - [x] `grep -rn "FilledButton\|OutlinedButton(" lib/features lib/shared` 零残留（SegmentedButton 除外；另收敛 member_manager/share_invite_sheet/lock_gate/currency_manage_page/csv_import_page/voice_entry_sheet/color_picker_dialog 共 12 文件 19 处）
  - [x] 受影响页面 widget 测试通过；Golden 差异在 0.5% 容差内（24 基线全过，无需重建）
- **涉及文件**：见映射表（5 个业务文件 + `appearance_page.dart` 1 处）

---

#### BK-A-006：静态卡点升级（全 lib/ + ARGB 字面量规则）

- **任务描述（imperative）**：Extend the UI token lint gate to all of lib/ and add an ARGB-literal rule with a registered whitelist（静态卡点扩至全 lib/ 并新增 ARGB 字面量规则与白名单登记）
- **任务描述（present continuous）**：Extending the UI token lint gate coverage and rules（正在升级静态卡点）
- **Status**：`completed`（2026-08-14，全 lib/ 扫描 0 违规 + 植入自测可拦截）
- **Dependencies**：blockedBy：无；blocks：BK-A-010
- **对应规范文档**：`docs/15-UI重构审核改进Spec规格说明.md` §4.5（F5/A1）；上游：`docs/10-UI重构Spec规格说明.md` §9 静态层、`docs/09-UI设计文档-主题与视觉体系.md` §6.4
- **Checklist**：
  - [x] `tool/check_ui_tokens.dart` 扫描根改为 `lib/`
  - [x] 规则表新增 `0x[0-9A-Fa-f]{8}\b` ARGB 字面量模式（不误伤短 hex 如 `0x2D`）
  - [x] 白名单逐条登记并注明理由：4 个 Token/主题定义文件、取色器 hue 色带、分类业务色 `_palette`（另登记 sync_merger 默认分类色、settings_repository hex 解析两处）
  - [x] 全量扫描退出码 0；人为植入违规（features/ 与 shared/widgets/ 各一处）自测可拦截后回滚
  - [x] CI 链路（`dart run tool/check_ui_tokens.dart`）无需改动，确认通过（注：Dart 3.12+ main 返回值不再映射进程退出码，脚本已改显式 `exit(1)` 保证 CI 卡点真正生效）
- **涉及文件**：`app/tool/check_ui_tokens.dart`

---

#### BK-A-007：备份排除 bg_* 键

- **任务描述（imperative）**：Exclude background preference keys from encrypted backups and add regression coverage（备份排除 `bg_*` 偏好键并补回归用例）
- **任务描述（present continuous）**：Excluding background preference keys from backups（正在收口备份范围）
- **Status**：`completed`（2026-08-14，排除生效 + 回归用例全绿）
- **Dependencies**：blockedBy：无；blocks：BK-A-010
- **对应规范文档**：`docs/15-UI重构审核改进Spec规格说明.md` §4.6（F6/S1/S3）；上游：`docs/10-UI重构Spec规格说明.md` §8 隐私
- **Checklist**：
  - [x] `backup_service.dart` `_excludedMetaPrefixes` 增加 `'bg_'`
  - [x] 新回归用例：写入 `bg_enabled`/`bg_image_path` → 创建备份 → 恢复至新库 → `backgroundSettings()` 回退默认
  - [x] `background_settings_test.dart` 补 `bg_overlay_mode` 非法值回退 auto 用例
  - [x] 既有备份用例（14 表结构断言、口令错误分支）不受影响，全部通过
- **涉及文件**：`app/lib/features/backup/backup_service.dart`、`app/test/unit/features/backup/backup_service_test.dart`、`app/test/unit/features/theme/background_settings_test.dart`

---

#### BK-A-008：秒开入口接入统一 shell builder

- **任务描述（imperative）**：Route the quick-entry cold-start branch through the shared ThemeTransition + AppBackground shell builder（秒开分支接入统一 shell builder，背景观感与主入口一致）
- **任务描述（present continuous）**：Routing the quick-entry cold-start branch through the shared shell builder（正在统一秒开入口 builder 链）
- **Status**：`completed`（2026-08-14，共用 builder 链 + 冒烟断言通过；人工走查项待真机一并执行）
- **Dependencies**：blockedBy：无（前置决策 D1 已在 15 文档 §4.7 给出结论：纳入）；blocks：BK-A-010
- **对应规范文档**：`docs/15-UI重构审核改进Spec规格说明.md` §4.7（F7/A2，含决策 D1）；上游：`docs/09-UI设计文档-主题与视觉体系.md` §5.1
- **Checklist**：
  - [x] `app.dart` 导出共用 `appShellBuilder`（ThemeTransition + AppBackground + lockGate）
  - [x] `main.dart` 秒开分支与主分支 builder 均改调 `appShellBuilder`
  - [x] `app_smoke_test.dart` 增加秒开分支冒烟断言
  - [ ] 人工走查：秒开模式设置背景后 QuickEntrySheet 底层背景 + 遮罩可见（随 BK-A-009 真机环节执行）
- **涉及文件**：`app/lib/app.dart`、`app/lib/main.dart`、`app/test/widget/app_smoke_test.dart`

---

### W3 真机验证（08-19，视设备可用性浮动）

---

#### BK-A-009：真机集成测试与性能验证

- **任务描述（imperative）**：Run the background integration test and performance benchmarks on Android and iOS devices（真机执行背景集成测试与三项性能指标实测）
- **任务描述（present continuous）**：Running on-device integration and performance verification（正在执行真机验证）
- **Status**：`blocked`（本环境无 Android/iOS 真机设备；阻塞条件：需可用真机或模拟器 + 相册交互；代码前置 BK-A-001/002/003 已全部完成，P1 预缓存优化已随 BK-A-003 落地）
- **Dependencies**：blockedBy：BK-A-001、BK-A-002、BK-A-003；blocks：BK-A-010
- **对应规范文档**：`docs/15-UI重构审核改进Spec规格说明.md` §4.8（F8/P1–P3）；上游：`docs/10-UI重构Spec规格说明.md` §8/§9、`docs/11-UI重构任务分解.md` BK-UI-017/018
- **Checklist**：
  - [ ] `flutter test integration_test/background_test.dart -d <android>` 全步骤通过（含新增「选择背景图片」断言）——**待真机**
  - [ ] iOS 同上 1 轮——**待真机**
  - [ ] 性能实测并记录：主题切换全树重建 ≤100ms（profile timeline）；选图到生效 ≤800ms（含 `precacheImage` 预缓存优化落地，代码已随 BK-A-003 提交）；release 包体增量 ≤300KB——**待真机**
  - [ ] 五档亮度实图（极亮/亮/中调/暗/极暗）观感走查，账单列表可读——**待真机**
  - [ ] 测试输出、timeline 截图、包体 diff 归档备查——**待真机**
- **涉及文件**：`app/integration_test/background_test.dart`（随 BK-A-001 更新）、`app/lib/shared/theme/background/background_controller.dart`（P1 预缓存优化）

---

### W4 收尾（08-20）

---

#### BK-A-010：文档状态回填与验收

- **任务描述（imperative）**：Backfill document statuses and close the audit findings with repair evidence（回填文档状态、以修复证据闭环 14 审核不符合项）
- **任务描述（present continuous）**：Backfilling document statuses and closing audit findings（正在回填文档并闭环审核项）
- **Status**：`completed`（2026-08-14；BK-A-009 因设备不可用保持 blocked，11/13/14 文档已按双层标记回填并保留待真机说明）
- **Dependencies**：blockedBy：BK-A-001 ~ BK-A-009（009 因设备阻塞时，可先回填代码类 7 项并在 009 处保留 blocked 说明）；blocks：无
- **对应规范文档**：`docs/15-UI重构审核改进Spec规格说明.md` §4.8（F8/A3）；回填对象：`docs/11-UI重构任务分解.md`、`docs/13-UI重构验收报告.md`、`docs/14-UI重构审核报告.md`
- **Checklist**：
  - [x] 13 报告 §4 待真机表补"实测结果"列并填入 BK-A-009 数据（阻塞说明）
  - [x] 11 文档 BK-UI-017/018 状态改为「代码 done / 真机 blocked」双层标记（与事实一致）
  - [x] 14 报告不符合项 #1~#8 逐条标注修复 commit 与验证证据
  - [x] 本文件全部任务状态回填（009 blocked 并注明阻塞条件）
  - [ ] 里程碑评审：对照 §4 验收标准逐项签字（009 阻塞项除外，代码类验收项已完成）
- **涉及文件**：`docs/11-UI重构任务分解.md`、`docs/13-UI重构验收报告.md`、`docs/14-UI重构审核报告.md`、本文件

---

## 4. 排期甘特

```
任务            08-12  08-13  08-14  08-15  08-16  08-17  08-18  08-19  08-20
W1 阻断修复     ████████████████
W2 质量补强                            ████████████████████
W3 真机验证                                                    ████（视设备浮动）
W4 收尾                                                                          ████
里程碑                   M1▲                          M2▲              M3▲     M4▲
```

- **M1（08-14）**：P0/P1 修复完成，背景链路 UI 全流程可用且有真实路径测试保护
- **M2（08-18）**：质量补强收口，CI 静态卡点升级生效，备份隐私边界闭合
- **M3（08-19）**：真机证据归档（设备阻塞时顺延并标 blocked）
- **M4（08-20）**：文档状态与事实一致，14 审核不符合项全部闭环

## 5. 验收标准（全局）

1. ✅ 全新安装设备上可通过 UI 完成「选主题 + 首次设背景 + 调遮罩」全流程（F1/F2 闭环）——widget 真实交互用例 + 集成测试断言链覆盖。
2. ✅ 渲染链路无相对路径文件读取；兼容迁移路径有单测断言保护（F2/F3）。
3. ✅ `lib/` 全量静态扫描 0 裸值违规，白名单逐条有理由（F5）。
4. ✅ 加密备份不含 `bg_*` 键，跨设备恢复无悬空背景态（F6）。
5. ⛔ 真机三指标（≤100ms / ≤800ms / ≤300KB）实测达标并回填 13 报告（F8）——**待真机（BK-A-009 blocked）**，代码侧预缓存优化已落地。
6. ✅ 11/13/14 文档状态与事实一致，无"待真机标 done"类状态漂移（F8/A3，双层标记）。

## 6. 风险缓冲

| 风险 | 影响 | 处置 |
| --- | --- | --- |
| 真机设备不可用 | BK-A-009/010 延期 | 009 标 blocked 并记录条件；代码类任务先行全部收口，不阻塞 M1/M2 |
| Golden 基线漂移超预期 | BK-A-005 延期 | 逐张人工评审；确属变体替换的合理变化则 `--update-goldens` 重建并在提交说明中附 diff 摘要 |
| lint 扩范围暴露存量 | BK-A-006 延期 | 白名单过渡；存量逐条登记，新增零容忍 |
| 与既有并行工作冲突 | 文件冲突 | `appearance_page.dart` 被 001/003/005 三任务触及，单人串行执行；并行开发时该文件归本波次独占 |

---

*维护规则：任务状态变更即时回填本表；与 BK-UI/BK-R 系列并行时注意冲突文件清单（本波次独占 `appearance_page.dart`、`tool/check_ui_tokens.dart`）。*
