# 33-全面代码审查任务分解

| 项 | 内容 |
|---|---|
| 文档编号 | BK-REV-2026-WBS |
| 对应 Spec | [32-全面代码审查改进Spec规格说明](./32-全面代码审查改进Spec规格说明.md) |
| 版本 | v1.0 |
| 日期 | 2026-08-27 |
| 状态约定 | `pending` / `in_progress` / `completed` / `blocked` |

**图例**

- **blockedBy**：依赖的前置任务 ID
- **blocks**：本任务完成后的解锁任务
- **Imperative**：PR 标题用语
- **Present continuous**：进行中文案用语

---

## 任务总览

| ID | 标题 | 优先级 | Status | blockedBy | blocks |
|---|---|---|---|---|---|
| BK-R01 | Fix rate_snapshot loss in sync merger | P0 | completed | — | BK-R17, BK-R18 |
| BK-R02 | Fix findDuplicate timezone truncation | P0 | completed | — | — |
| BK-R03 | Include child categories in budget spent | P0 | completed | — | — |
| BK-R04 | Prevent owner demotion via accept-invite | P0 | completed | — | — |
| BK-R05 | Whitelist backup restore column names | P0 | completed | — | — |
| BK-R06 | Enforce HTTPS for sync API | P0 | completed | — | — |
| BK-R07 | Add PIN unlock failure cooldown | P0 | completed | — | — |
| BK-R08 | Mask amounts in bill detail sheet | P0 | completed | — | — |
| BK-R09 | Wrap autoCreate book inserts in transaction | P0 | completed | — | — |
| BK-R10 | Make refresh token rotation atomic | P0 | completed | — | — |
| BK-R11 | Group bills using local calendar day | P1 | completed | — | — |
| BK-R12 | Preserve note when creating transfer | P1 | completed | — | — |
| BK-R13 | Guard Money.convert against int overflow | P1 | completed | — | — |
| BK-R14 | Restrict biometric to biometricOnly | P1 | completed | — | — |
| BK-R15 | Exclude privacy_ keys from backup | P1 | completed | BK-R05 | — |
| BK-R16 | Stop silent register after login 401 | P1 | completed | — | BK-R06 |
| BK-R17 | Replay/align FK handling on merger update | P1 | completed | BK-R01 | — |
| BK-R18 | Support book entity update in merger | P1 | completed | BK-R01 | — |
| BK-R19 | Add transaction query indexes via migration | P1 | completed | — | BK-R20 |
| BK-R20 | Windowed loading for bills list | P1 | completed | BK-R19 | — |
| BK-R21 | Localize scroll setState; cache ThemeData | P1 | completed | — | — |
| BK-R22 | Lazy-mount reports tab data | P1 | completed | — | — |
| BK-R23 | Make CategoryPicker scrollable | P1 | completed | — | — |
| BK-R24 | Fix over-escaping relative import | P1 | completed | — | — |
| BK-R25 | Rate-limit unauthenticated sync + IPv6 auth keys | P1 | completed | — | — |
| BK-R26 | Fail-closed book role default | P2 | completed | — | — |
| BK-R27 | Recurring FKs + installment dedupe key | P2 | completed | — | — |
| BK-R28 | Batch currency seed; exists() for deletes | P2 | completed | — | — |
| BK-R29 | Deduplicate rate provider and HTTP client | P2 | completed | — | — |
| BK-R30 | Tighten lints; stop swallowing errors; pin JWT algs | P2 | completed | — | — |

---

## P0 任务详情

### BK-R01 — Fix rate_snapshot loss in sync merger

| 字段 | 内容 |
|---|---|
| **Status** | pending |
| **Priority** | P0 |
| **Imperative** | Fix rate_snapshot loss in sync merger |
| **Present continuous** | Fixing rate_snapshot loss in sync merger |
| **Description** | 在 `_createTransaction` / `_updateTransaction` 提取并写入 payload 中的 `rate_snapshot`，与本地 enqueue 字段对齐，避免多币种同步后汇率被重置为 1:1。 |
| **blockedBy** | — |
| **blocks** | BK-R17, BK-R18 |
| **Spec** | [Spec §3 R-01](./32-全面代码审查改进Spec规格说明.md#r-01-合并流水丢失-rate_snapshot) |

**Checklist**

- [ ] `_createTransaction` 写入 `rateSnapshot: Value(_int(p, 'rate_snapshot') ?? kRateScale)`
- [ ] `_updateTransaction` 用 `_intField` 提取 `rate_snapshot` 并写入 companion
- [ ] 本地 payload 已含字段（`transaction_repository.dart` enqueue）无需改
- [ ] 新增/更新 `sync_merger_test.dart`：create 往返、update 往返
- [ ] `flutter test test/unit/features/sync/sync_merger_test.dart` 通过

**Files**

- `app/lib/features/sync/sync_merger.dart`
- `app/test/unit/features/sync/sync_merger_test.dart`

---

### BK-R02 — Fix findDuplicate timezone truncation

| 字段 | 内容 |
|---|---|
| **Status** | pending |
| **Priority** | P0 |
| **Imperative** | Fix findDuplicate timezone truncation |
| **Present continuous** | Fixing findDuplicate timezone truncation |
| **Description** | `findDuplicate` 将本地 DateTime 分量直接包成 UTC，导致非 UTC 时区下分钟窗口偏移、导入去重失效。 |
| **blockedBy** | — |
| **blocks** | — |
| **Spec** | Spec R-02 |

**Checklist**

- [ ] `occurredAt` 先 `toUtc()` 再截断分量
- [ ] 单测覆盖 UTC+8 同分钟去重命中、跨分钟不命中
- [ ] 手工：CSV 导入重复行只写一条
- [ ] `flutter test test/unit/data/repositories/transaction_repository_test.dart` 通过

**Files**

- `app/lib/data/repositories/transaction_repository.dart`
- `app/test/unit/data/repositories/transaction_repository_test.dart`

---

### BK-R03 — Include child categories in budget spent

| 字段 | 内容 |
|---|---|
| **Status** | pending |
| **Priority** | P0 |
| **Imperative** | Include child categories in budget spent |
| **Present continuous** | Including child categories in budget spent |
| **Description** | `spentForPeriod` 注释声称含子分类，SQL 仅精确匹配；父分类预算漏计子分类支出。 |
| **blockedBy** | — |
| **blocks** | — |
| **Spec** | Spec R-03 |

**Checklist**

- [ ] SQL 增加 `category_id IN (SELECT id FROM categories WHERE parent_id = ?)`
- [ ] 保留 `deleted_at IS NULL` 过滤
- [ ] 单测：父预算 + 两子类流水 = 合计；删除子类不计
- [ ] `flutter test test/unit/data/repositories/budget_repository_test.dart` 通过

**Files**

- `app/lib/data/repositories/budget_repository.dart`
- `app/test/unit/data/repositories/budget_repository_test.dart`

---

### BK-R04 — Prevent owner demotion via accept-invite

| 字段 | 内容 |
|---|---|
| **Status** | pending |
| **Priority** | P0 |
| **Imperative** | Prevent owner demotion via accept-invite |
| **Present continuous** | Preventing owner demotion via accept-invite |
| **Description** | `accept-invite` 的 upsert 会覆盖已有角色；owner 接受 viewer/editor 邀请后失去 owner。 |
| **blockedBy** | — |
| **blocks** | — |
| **Spec** | Spec R-04 |

**Checklist**

- [ ] 事务内查询现有 role；owner 时角色保持不变（仍标记 token used 或拒绝并保留 token——推荐：角色不变 + token used + 200）
- [ ] 非 owner：保持现逻辑 upsert
- [ ] 集成测试：owner 接受 viewer 邀请后 role 仍为 owner
- [ ] 集成测试：editor 接受 viewer 邀请后变为 viewer
- [ ] `cd server && npm run test:integration` 通过

**Files**

- `server/src/books/books.routes.ts`
- `server/tests/books.routes.integration.test.ts`

---

### BK-R05 — Whitelist backup restore column names

| 字段 | 内容 |
|---|---|
| **Status** | pending |
| **Priority** | P0 |
| **Imperative** | Whitelist backup restore column names |
| **Present continuous** | Whitelisting backup restore column names |
| **Description** | 恢复时列名来自 JSON map keys 且直接拼进 INSERT，存在恶意备份 SQL 注入面。 |
| **blockedBy** | — |
| **blocks** | BK-R15 |
| **Spec** | Spec R-05 |

**Checklist**

- [ ] 定义每表合法列名白名单（与 Drift schema 对齐）
- [ ] 恢复时过滤/拒绝未知列名，抛 `BackupCipherException`
- [ ] 单测：恶意列名备份恢复失败且原库数据不变
- [ ] 正常备份恢复行数正确

**Files**

- `app/lib/features/backup/backup_service.dart`
- `app/test/unit/features/backup/backup_service_test.dart`

---

### BK-R06 — Enforce HTTPS for sync API

| 字段 | 内容 |
|---|---|
| **Status** | pending |
| **Priority** | P0 |
| **Imperative** | Enforce HTTPS for sync API |
| **Present continuous** | Enforcing HTTPS for sync API |
| **Description** | 同步基址硬编码 `http://localhost:3000`，`HttpSyncApi` 无 scheme 校验，凭据可能明文传输。 |
| **blockedBy** | — |
| **blocks** | BK-R16 |
| **Spec** | Spec R-06 |

**Checklist**

- [ ] `HttpSyncApi` 构造或 `_request` 增加 secure 断言
- [ ] debug / `localhost` / `127.0.0.1` 允许 http；其余必须 https
- [ ] 支持 `String.fromEnvironment('SERVER_URL')` 注入
- [ ] 单测覆盖 allow/deny 矩阵
- [ ] README/开发文档说明生产配置

**Files**

- `app/lib/core/constants/constants.dart`
- `app/lib/features/sync/sync_api.dart`
- `app/lib/features/sync/sync_providers.dart`
- `app/test/unit/features/sync/sync_api_test.dart`（或新建）

---

### BK-R07 — Add PIN unlock failure cooldown

| 字段 | 内容 |
|---|---|
| **Status** | pending |
| **Priority** | P0 |
| **Imperative** | Add PIN unlock failure cooldown |
| **Present continuous** | Adding PIN unlock failure cooldown |
| **Description** | PIN 失败无次数限制与冷却，6 位数字可被暴力尝试。 |
| **blockedBy** | — |
| **blocks** | — |
| **Spec** | Spec R-07 |

**Checklist**

- [ ] `app_meta` 持久化 `privacy_pin_fails` / `privacy_pin_lock_until`
- [ ] ≥5 失败锁 30s；≥10 失败锁 5min
- [ ] 解锁成功清零计数
- [ ] 锁定期内 `verifyPin` 直接 false 或专用 `isLockedOut`
- [ ] UI 提示剩余冷却秒数
- [ ] 单测：计数、冷却、成功清零、widget 重建不重置

**Files**

- `app/lib/data/repositories/lock_repository.dart`
- `app/lib/features/auth_lock/lock_controller.dart`
- `app/lib/features/auth_lock/lock_gate.dart`
- `app/test/unit/data/repositories/lock_repository_test.dart`
- `app/test/unit/features/auth_lock/lock_controller_test.dart`

---

### BK-R08 — Mask amounts in bill detail sheet

| 字段 | 内容 |
|---|---|
| **Status** | pending |
| **Priority** | P0 |
| **Imperative** | Mask amounts in bill detail sheet |
| **Present continuous** | Masking amounts in bill detail sheet |
| **Description** | 详情金额写死 `masked: false`，后台系统快照可泄露真实金额。 |
| **blockedBy** | — |
| **blocks** | — |
| **Spec** | Spec R-08 |

**Checklist**

- [ ] `bill_detail_sheet` 接 `amountMaskProvider`
- [ ] 检查 `quick_entry_sheet` / `capture_confirm_page` 是否需同样处理
- [ ] Widget 测试：masked 时显示脱敏字符
- [ ] 手工：开详情 → 切后台 → 快照无明文金额

**Files**

- `app/lib/features/bills/bill_detail_sheet.dart`
- `app/lib/features/quick_entry/quick_entry_sheet.dart`（视检查结果）
- `app/lib/features/auto_capture/capture_confirm_page.dart`（视检查结果）

---

### BK-R09 — Wrap autoCreate book inserts in transaction

| 字段 | 内容 |
|---|---|
| **Status** | pending |
| **Priority** | P0 |
| **Imperative** | Wrap autoCreate book inserts in transaction |
| **Present continuous** | Wrapping autoCreate book inserts in transaction |
| **Description** | 首推自动建账的两条 INSERT 不在同一事务，可能产生无 owner 孤儿账本。 |
| **blockedBy** | — |
| **blocks** | — |
| **Spec** | Spec R-09 |

**Checklist**

- [ ] 使用 `withTransaction` 包裹 books + book_members INSERT
- [ ] 保留 ON CONFLICT 与 race 成员检查
- [ ] 单测/集成：第二步失败不残留 books 行
- [ ] `server` tsc + jest 通过

**Files**

- `server/src/auth/book.middleware.ts`
- `server/tests/`（适当用例）

---

### BK-R10 — Make refresh token rotation atomic

| 字段 | 内容 |
|---|---|
| **Status** | pending |
| **Priority** | P0 |
| **Imperative** | Make refresh token rotation atomic |
| **Present continuous** | Making refresh token rotation atomic |
| **Description** | 先撤销旧 refresh 再签发新 token，中间失败导致用户丢失刷新能力。 |
| **blockedBy** | — |
| **blocks** | — |
| **Spec** | Spec R-10 |

**Checklist**

- [ ] 撤销 + 签发放入同一 `withTransaction`
- [ ] 清理过期 token 保持不阻断轮换（可事务外）
- [ ] 集成测试：轮换成功后旧 token 401、新 token 可用
- [ ] 失败路径整体回滚或旧 token 仍有效

**Files**

- `server/src/auth/tokens.ts`
- `server/tests/auth.routes.integration.test.ts`

---

## P1 任务详情（精简格式）

### BK-R11 — Group bills using local calendar day

- **Status**: pending · **Priority**: P1
- **Imperative**: Group bills using local calendar day
- **Present continuous**: Grouping bills using local calendar day
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-11

**Checklist**

- [ ] `groupBillsByDay` 先 `toLocal()`
- [ ] 与列表行 `toLocal()` 日界一致
- [ ] 单测：UTC 存储 + 本地时区跨日边界

**Files**: `app/lib/features/bills/bills_grouping.dart`, tests

---

### BK-R12 — Preserve note when creating transfer

- **Status**: pending · **Priority**: P1
- **Imperative**: Preserve note when creating transfer
- **Present continuous**: Preserving note when creating transfer
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-12

**Checklist**

- [ ] `createTransfer` 增加 `note` 并入双流水 payload
- [ ] 控制器保存路径传入 note
- [ ] 单测：创建后两侧 note 正确

**Files**: `transaction_repository.dart`, `quick_entry_controller.dart`, tests

---

### BK-R13 — Guard Money.convert against int overflow

- **Status**: pending · **Priority**: P1
- **Imperative**: Guard Money.convert against int overflow
- **Present continuous**: Guarding Money.convert against int overflow
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-13

**Checklist**

- [ ] 乘法前检测溢出并抛 `ArgumentError`（或降级策略文档化）
- [ ] 边界单测：`kMaxAmountMinor` × 大汇率

**Files**: `app/lib/core/utils/money.dart`, money tests

---

### BK-R14 — Restrict biometric to biometricOnly

- **Status**: pending · **Priority**: P1
- **Imperative**: Restrict biometric to biometricOnly
- **Present continuous**: Restricting biometric to biometricOnly
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-14

**Checklist**

- [ ] `AuthenticationOptions(biometricOnly: true)`
- [ ] 设备无生物识别时仍可回退 PIN（available 检查不变）

**Files**: `app/lib/features/auth_lock/biometric.dart`

---

### BK-R15 — Exclude privacy_ keys from backup

- **Status**: pending · **Priority**: P1
- **Imperative**: Exclude privacy_ keys from backup
- **Present continuous**: Excluding privacy_ keys from backup
- **blockedBy**: BK-R05 · **blocks**: —
- **Spec**: Spec R-15

**Checklist**

- [ ] `_excludedMetaPrefixes` 增加 `'privacy_'`
- [ ] 单测：备份不含 pin hash；恢复不写入 privacy_ 键

**Files**: `backup_service.dart`, `backup_service_test.dart`

---

### BK-R16 — Stop silent register after login 401

- **Status**: pending · **Priority**: P1
- **Imperative**: Stop silent register after login 401
- **Present continuous**: Stopping silent register after login 401
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-16（与 BK-R06 可并行，建议 HTTPS 先合）

**Checklist**

- [ ] login 401 不再自动 register
- [ ] UI 区分「密码错误」与「账户不存在」并引导注册
- [ ] `_ensureTokens` 仅在有存储 token 或明确 re-login 时工作
- [ ] 单测：错密码不创建账户

**Files**: `sync_providers.dart`, `sync_engine.dart`, tests

---

### BK-R17 — Replay/align FK handling on merger update

- **Status**: pending · **Priority**: P1
- **Imperative**: Align FK handling on merger update path
- **Present continuous**: Aligning FK handling on merger update path
- **blockedBy**: BK-R01 · **blocks**: —
- **Spec**: Spec R-17

**Checklist**

- [ ] account 缺失：入重放/与 create 同策略，禁止静默丢
- [ ] category 缺失：跳过或重放，禁止置 null
- [ ] 合并循环 `catch` 至少 debugPrint entity/id

**Files**: `sync_merger.dart`, `sync_merger_test.dart`

---

### BK-R18 — Support book entity update in merger

- **Status**: pending · **Priority**: P1
- **Imperative**: Support book entity update in merger
- **Present continuous**: Supporting book entity update in merger
- **blockedBy**: BK-R01 · **blocks**: —
- **Spec**: Spec R-18

**Checklist**

- [ ] `_localIdByRemoteId` 支持 book
- [ ] `_updateBook` 更新 name/type
- [ ] 单测：远端改名同步到本地

**Files**: `sync_merger.dart`, tests

---

### BK-R19 — Add transaction query indexes via migration

- **Status**: pending · **Priority**: P1
- **Imperative**: Add transaction query indexes via migration
- **Present continuous**: Adding transaction query indexes via migration
- **blockedBy**: — · **blocks**: BK-R20
- **Spec**: Spec R-19

**Checklist**

- [ ] schema v8+ 迁移：`(book_id, occurred_at)`、`(remote_id)`、`(account_id)`、报表组合索引
- [ ] `migrations_test.dart` 覆盖升版
- [ ] 无破坏性操作

**Files**: `transactions_table.dart`, `database.dart`, `migrations_test.dart`

---

### BK-R20 — Windowed loading for bills list

- **Status**: pending · **Priority**: P1
- **Imperative**: Windowed loading for bills list
- **Present continuous**: Implementing windowed loading for bills list
- **blockedBy**: BK-R19 · **blocks**: —
- **Spec**: Spec R-20

**Checklist**

- [ ] 列表按时间窗/limit 加载 + 加载更多
- [ ] 首屏与现有分组 UI 兼容
- [ ] 性能抽测 10k 行首屏

**Files**: `bills_providers.dart`, `bills_page.dart`, `transaction_repository.dart`

---

### BK-R21 — Localize scroll setState; cache ThemeData

- **Status**: pending · **Priority**: P1
- **Imperative**: Localize scroll setState and cache ThemeData
- **Present continuous**: Localizing scroll setState and caching ThemeData
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-21

**Checklist**

- [ ] `_scrolled` 不再触发整棵 `MaterialApp` 重建
- [ ] `materialThemesFor` 记忆化
- [ ] 滚动阈值切换目视无掉帧回归

**Files**: `app/lib/app.dart`, `theme_controller.dart`

---

### BK-R22 — Lazy-mount reports tab data

- **Status**: pending · **Priority**: P1
- **Imperative**: Lazy-mount reports tab data
- **Present continuous**: Lazy-mounting reports tab data
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-22

**Checklist**

- [ ] 仅进入报表 Tab 后才 watch 图表 provider
- [ ] 冷启动停在账单时不跑报表 SQL

**Files**: `app/lib/app.dart`, `reports_page.dart`

---

### BK-R23 — Make CategoryPicker scrollable

- **Status**: pending · **Priority**: P1
- **Imperative**: Make CategoryPicker scrollable
- **Present continuous**: Making CategoryPicker scrollable
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-23

**Checklist**

- [ ] 外包可滚动容器
- [ ] 快速记账弹层 60% 高度下无 overflow
- [ ] Widget 测试：分类多时不报溢出

**Files**: `shared/widgets/category_picker.dart` 与 `quick_entry_sheet.dart`（若需）

---

### BK-R24 — Fix over-escaping relative import

- **Status**: pending · **Priority**: P1
- **Imperative**: Fix over-escaping relative import
- **Present continuous**: Fixing over-escaping relative import
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-24

**Checklist**

- [ ] 改为 `import '../../shared/widgets/glass_nav.dart';`
- [ ] `flutter analyze` 0 issues

**Files**: `capture_confirm_page.dart`

---

### BK-R25 — Rate-limit unauthenticated sync + IPv6 auth keys

- **Status**: pending · **Priority**: P1
- **Imperative**: Rate-limit unauthenticated sync and normalize auth IP keys
- **Present continuous**: Rate-limiting unauthenticated sync and normalizing auth IP keys
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-25

**Checklist**

- [ ] `/sync` 鉴权前 IP 限流
- [ ] auth 路由使用 `ipKeyGenerator`
- [ ] 集成测试 429 行为保持

**Files**: `server/src/app.ts`, `auth.routes.ts`, `sync.routes.ts`

---

## P2 任务详情（精简）

### BK-R26 — Fail-closed book role default

- **Status**: pending · **Priority**: P2
- **Imperative**: Fail-closed on missing book role
- **Present continuous**: Failing closed on missing book role
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-26

**Checklist**: [ ] `roleOf` 默认 `viewer`；[ ] 相关单测；[ ] 共享账本离线写入被拦

**Files**: `book_repository.dart`, tests

---

### BK-R27 — Recurring FKs + installment dedupe key

- **Status**: pending · **Priority**: P2
- **Imperative**: Add recurring FKs and fix installment dedupe key
- **Present continuous**: Adding recurring FKs and fixing installment dedupe key
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-27

**Checklist**: [ ] 表定义加 references + 迁移；[ ] 去重不依赖 note；[ ] 改名计划不双记；[ ] 单测

**Files**: `recurring_tables.dart`, `database.dart`, `recurring_service.dart`

---

### BK-R28 — Batch currency seed; exists() for deletes

- **Status**: pending · **Priority**: P2
- **Imperative**: Batch currency seed and use exists for delete guards
- **Present continuous**: Batching currency seed and using exists for delete guards
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-28

**Checklist**: [ ] 币种 seed 批量 insert；[ ] 账户/分类删除存在性 limit(1)；[ ] 首启耗时对比

**Files**: `currency_repository.dart`, `account_repository.dart`, `category_repository.dart`

---

### BK-R29 — Deduplicate rate provider and HTTP client

- **Status**: pending · **Priority**: P2
- **Imperative**: Deduplicate rate provider and HTTP client helpers
- **Present continuous**: Duplicating rate provider and HTTP client helpers
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-29

**Checklist**: [ ] 单一 `exchangeRateServiceProvider`；[ ] 公共 `_request/_json`；[ ] 无双实例

**Files**: providers, `sync_api.dart`, `books_api.dart`

---

### BK-R30 — Tighten lints; stop swallowing errors; pin JWT algs

- **Status**: pending · **Priority**: P2
- **Imperative**: Tighten lints, log swallowed errors, pin JWT algorithms
- **Present continuous**: Tightening lints, logging swallowed errors, pinning JWT algorithms
- **blockedBy**: — · **blocks**: —
- **Spec**: Spec R-30

**Checklist**: [ ] analysis_options strict 三件套；[ ] 关键 catch 打日志；[ ] `jwt.verify` `algorithms: ['HS256']`；[ ] 删死常量 `REFRESH_TTL_DAYS` 等

**Files**: `analysis_options.yaml`, 多处 catch, `server/src/auth/middleware.ts`, `tokens.ts`

---

## 推荐实施顺序（Wave）

```
Wave 1（并行安全/数据 P0）
  BK-R01  BK-R02  BK-R03  BK-R04  BK-R05  BK-R08  BK-R09  BK-R10

Wave 2（传输/锁定 P0 + 阻塞链）
  BK-R06 → BK-R16
  BK-R07
  BK-R05 → BK-R15

Wave 3（P1 同步与性能）
  BK-R11  BK-R12  BK-R13  BK-R14  BK-R17  BK-R18
  BK-R19 → BK-R20
  BK-R21  BK-R22  BK-R23  BK-R24  BK-R25

Wave 4（P2 打磨）
  BK-R26  BK-R27  BK-R28  BK-R29  BK-R30
```

> 开工时将当前波次首个任务置为 `in_progress`，完成后立刻 `completed` 并启动解锁的后续任务，保证始终至少有一个 `in_progress`。

---

## 验收门禁（整包完成时）

```bash
cd server && npx tsc --noEmit && npm test && npm run test:integration
cd app && flutter analyze && dart run tool/check_ui_tokens.dart && flutter test
```

- [ ] P0 全部 completed
- [ ] Spec §6.3 安全门禁清单全勾
- [ ] Spec §8 进度表无 pending P0
- [ ] 新增测试均在 CI 路径可跑
